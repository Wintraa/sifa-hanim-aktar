# Şifa Hanım Aktar — React + Express + SQLite

Eski HTML/JS sitesi kökte duruyor. Yeni mimari:

- `server/` → Express API (port 4000)
- `client/` → React Vite (port 5173)
- `database/bitki.db` → ortak SQLite

Detay: `server/README.md`
